# ==========================================
# PRODUCTION ENVIRONMENT CONFIGURATION
# ==========================================
spring.config.activate.on-profile=prod

# Database Connection (with env var overrides)
spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/career_path_adviser_new?useSSL=true&serverTimezone=UTC}
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:8010865586}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate Production Settings
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=false

# Production Logging Levels
logging.level.com.career=INFO
logging.level.org.springframework.security=WARN
logging.level.org.hibernate=WARN
logging.level.root=WARN
